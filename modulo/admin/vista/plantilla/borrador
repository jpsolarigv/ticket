</div> 
      
</div>
<!-- Libs JS -->
<script>
  $(document).ready(function () {
      // Inicializar la tabla DataTables
      var table = $('#example').DataTable({
          dom: 'rt',
          buttons: [
              {
                  extend: 'pdfHtml5',
                  text: 'Exportar a PDF',
                  className: 'btn-pdf ',
                  exportOptions: {
                      columns: ':visible'
                  }
              },
              {
                  extend: 'excelHtml5',
                  text: 'Exportar a Excel',
                  className: 'btn-excel',
                  exportOptions: {
                      columns: ':visible'
                  }
              },
              {
                  extend: 'csvHtml5',
                  text: 'Exportar a CSV',
                  className: 'btn-csv',
                  exportOptions: {
                      columns: ':visible'
                  }
              },
              {
                  extend: 'print',
                  text: 'Imprimir',
                  className: 'btn-print',
                  exportOptions: {
                      columns: ':visible'
                  }
              },
              {
                  extend: 'colvis',
                  text: 'Mostrar/Ocultar Columnas',
              }
          ]
      });

      // Configurar botones externos
      // Crear el botón "Colvis"
  new $.fn.dataTable.Buttons(table, {
      buttons: [
          {
              extend: 'colvis',
              text: 'Mostrar/Ocultar Columnas', // Texto interno
          }
      ]
  });

// Asociar botón personalizado al menú colvis
$('#colvisBtn').on('click', function () {
      $('.dt-button-collection').remove(); // Elimina colecciones previas
      table.button('.buttons-colvis').trigger(); // Activa el menú colvis
  });

  // Evitar que el menú colvis se inserte dentro de la tabla
  table.buttons().container().appendTo('body');
      
      $('#exportPdfBtn').on('click', function () {
          table.button('.buttons-pdf').trigger();
      });
      $('#exportExcelBtn').on('click', function () {
          table.button('.buttons-excel').trigger();
      });
      $('#exportCsvBtn').on('click', function () {
          table.button('.buttons-csv').trigger();
      });
      $('#printBtn').on('click', function () {
          table.button('.buttons-print').trigger();
      });
      $('#columnVisibilityBtn').on('click', function () {
          table.button('.buttons-colvis').trigger();
      });
  });
</script>


</body>
</html>        


<script>
      document.addEventListener("DOMContentLoaded", function() {
      const list = new List('table-default', {
      	sortClass: 'table-sort',
      	listClass: 'table-tbody',
      	valueNames: [ 'sort-name', 'sort-type', 'sort-city', 'sort-score',
      		{ attr: 'data-date', name: 'sort-date' },
      		{ attr: 'data-progress', name: 'sort-progress' },
      		'sort-quantity'
      	],
        page: 3, // Número de elementos por página
        pagination: true // Activar la paginación
      });

      // Opción adicional: Detectar eventos en la búsqueda
    const searchInput = document.querySelector('.search');
    searchInput.addEventListener('input', function () {
      list.search(searchInput.value);
    });

      })
    </script>

<script src="../../../../plantillas/tabler/libs/list/list.min.js"></script>


<!-- Vincular el archivo jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <!-- Vincular el archivo JS de DataTables -->
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <!-- Vincular el archivo JS de DataTables Buttons -->
    <script src="https://cdn.datatables.net/buttons/2.4.2/js/dataTables.buttons.min.js"></script>
    <!-- Vincular el archivo PDFMake -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/pdfmake.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/vfs_fonts.js"></script>
    <!-- Vincular el archivo JS para exportación a PDF, Excel, CSV -->
    <script src="https://cdn.datatables.net/buttons/2.4.2/js/buttons.html5.min.js"></script>
    <!-- Vincular el archivo JS para impresión -->
    <script src="https://cdn.datatables.net/buttons/2.4.2/js/buttons.print.min.js"></script>


    <script>
        $(document).ready(function () {
            // Inicializa la tabla con DataTables
            var table = $('#lista').DataTable({
                dom: 'Bfrtip', // Coloca los botones en la parte superior
                buttons: [
                    // Botón de exportación PDF configurado para ser accesible desde fuera de la tabla
                    {
                        extend: 'pdfHtml5',
                        text: 'PDF',
                        className: 'btn-pdf',
                        exportOptions: {
                            columns: ':visible' // Exportar solo las columnas visibles
                        },
                        customize: function (doc) {
                            doc.styles.tableHeader.fontSize = 14; // Tamaño de fuente para el encabezado
                            doc.styles.tableBodyEven.fontSize = 10; // Tamaño de fuente para el cuerpo de la tabla
                            doc.styles.tableBodyOdd.fontSize = 10; // Tamaño de fuente para el cuerpo de la tabla
                            doc.styles.tableHeader.bold = true; // Poner en negrita el encabezado
                        }
                    },
                    {
                        extend: 'excelHtml5',
                        text: 'Excel',
                        className: 'btn-excel',
                        exportOptions: {
                            columns: ':visible'
                        }
                    },  
                ]
            });

            // Configura el botón fuera de la tabla
            $('#exportPdfBtn').on('click', function () {
                // Llama al botón de exportación PDF desde fuera de la tabla
                table.button('.btn-pdf').trigger();
            });

            $('#exportExcelBtn').on('click', function () {
                table.button('.btn-excel').trigger();
            });

            $('#columnVisibilityBtn').on('click', function () {
                table.button('.btn-colvis').trigger();
            });
        });
    </script>

   

      